for walls: do different wall calc in render map condition:
if (y == 0)
  wall north;
if (x == 0)
  wall east;
if (y == len)
  wall south;
and corners
etc...





create map
parser map and inputs
create vars and img

init vars: map, width, height, mlx
//gets screen size
init vars: win (new window)

init img (new image), img addr (get_data_addr)
pacman: init (malloc), finds start, img (xpm_to_img), addr (get_data_addr)

map init: every texture: img (xpm_to_img), addr (get_data_addr)

render:
render where every texture goes (create_img)
(create_img gets each pixel and puts it)
render pacman: create_img pacman
put_screen: (clear_window and put_img_to_window)
