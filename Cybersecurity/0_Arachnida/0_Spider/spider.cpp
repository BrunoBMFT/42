#include "spider.hpp"

//script for removing ./data

int main(int ac, char **av) {
    if (ac < 2) {
        std::cout << "Not enough arguments" << std::endl;
		return (1);
    }

	// std::string url = av[ac - 1];
	// std::cout << "caught url: " << url << std::endl;

	


    return (0);
}